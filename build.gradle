// Removed licenser plugin - no longer maintained and incompatible with Gradle 9.4.1

defaultTasks 'shadowJar'

version '0.24.0'

subprojects {
    version = rootProject.version
    group = 'xyz.kyngs.librelogin'

    repositories {
        mavenLocal()
        mavenCentral()

        maven {
            url 'https://jitpack.io'
        }
    }

}
